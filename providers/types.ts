export type WalletData = {
    processing: boolean,
    wallet: string
}

export type MiscData = {
    bitcoin: number,
    ethereum: number,
    solana: number,
    light: boolean,
    currency: string
}